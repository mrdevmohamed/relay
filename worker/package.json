{
  "name": "openvpn-ws-relay-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker: WebSocket -> TCP bridge for opaque OpenVPN transport",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.19",
    "@cloudflare/workers-types": "^4.20250929.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8",
    "wrangler": "^3.95.0"
  }
}
